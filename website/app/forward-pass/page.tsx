import ForwardPassContent from '@/components/sections/ForwardPassContent'

export const metadata = {
  title: 'Forward Pass | Neural Networks for Equity Premium Prediction',
  description: 'Detailed guide to neural network forward pass: linear transformations, ReLU activation, dropout, and information flow through financial prediction models.',
}

export default function ForwardPassPage() {
  return <ForwardPassContent />
}