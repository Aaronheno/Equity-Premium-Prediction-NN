import BackpropagationContent from '@/components/sections/BackpropagationContent'

export const metadata = {
  title: 'Backpropagation | Neural Networks for Equity Premium Prediction',
  description: 'Complete guide to neural network backpropagation: chain rule application, gradient computation, error propagation, and automatic differentiation for financial models.',
}

export default function BackpropagationPage() {
  return <BackpropagationContent />
}