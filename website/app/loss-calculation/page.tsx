import LossCalculationContent from '@/components/sections/LossCalculationContent'

export const metadata = {
  title: 'Loss Calculation | Neural Networks for Equity Premium Prediction',
  description: 'Understanding neural network loss functions: MSE calculation, L1/L2 regularization, batch processing, and gradient computation for financial prediction models.',
}

export default function LossCalculationPage() {
  return <LossCalculationContent />
}